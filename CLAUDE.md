## Agent skills

### Issue tracker

Issues and specs are tracked as GitHub issues in `redouanend/MATHutrice` (via the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
